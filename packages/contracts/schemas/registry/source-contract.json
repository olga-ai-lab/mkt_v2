{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "olga://registry/source-contract",
  "title": "SourceContract",
  "description": "A verdade fisica e operacional de cada fonte que o agente le (Mestra §7.5). Existe porque 'freshness e parte da verdade' (Mestra §3): dado correto e desatualizado gera resposta falsa, e ate aqui o teto de idade era uma constante unica no codigo do retrieval, igual para o Brand Brain e para uma pagina de site. Fontes diferentes envelhecem de formas diferentes, e quem sabe disso e o dono da fonte, nao quem escreveu o retrieval.",
  "type": "object",
  "additionalProperties": false,
  "required": ["source_kind", "version", "status", "temporal_authority", "max_age_days", "default_quality", "carries_pii", "permission_scope", "owner"],
  "properties": {
    "source_kind": {
      "$ref": "olga://enums/source-kind",
      "description": "A mesma chave que mkt.evidence usa em source_kind. Nao e um vocabulario paralelo."
    },
    "version": { "type": "integer", "minimum": 1 },
    "status": { "enum": ["DRAFT", "CANDIDATE", "ACTIVE", "DEPRECATED", "BLOCKED"] },
    "temporal_authority": {
      "type": "string",
      "minLength": 1,
      "description": "Qual carimbo responde 'quando isto era verdade'. Nao e sempre o created_at: um Brand Brain vale a partir do activated_at, e uma pagina vale a partir do momento em que foi lida. Escolher o carimbo errado envelhece a fonte errada."
    },
    "max_age_days": {
      "type": ["integer", "null"],
      "minimum": 1,
      "description": "Acima disto a fonte esta vencida e o Validator emite SOURCE_STALE. `null` significa que esta fonte NAO vence — e isso e uma afirmacao, nao a ausencia de uma: o registro de uma marca no nosso proprio banco nao fica velho, ele fica errado, e errado nao se detecta por idade."
    },
    "default_quality": {
      "enum": ["HIGH", "MEDIUM", "LOW"],
      "description": "A qualidade que vale quando a propria linha nao declara uma. Estava fixada em 'HIGH' dentro do retrieval, para toda fatia, o que fazia uma pagina de site valer tanto quanto um registro nosso."
    },
    "carries_pii": {
      "type": "boolean",
      "description": "Se o conteudo desta fonte pode conter dado pessoal. O Mestra §23 pede classificacao antes de minimizacao e redacao; sem a classificacao as outras duas nao tem em que se apoiar."
    },
    "permission_scope": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Papeis que podem ler esta fonte atraves de um agente. Vazio significa que so o service_role alcanca."
    },
    "grain": {
      "type": "string",
      "description": "O que uma unidade desta fonte representa: uma versao de marca, uma pagina lida, uma linha de evidence."
    },
    "caveats": {
      "type": "array",
      "items": { "type": "string" },
      "description": "O que quem le precisa saber e o dado nao diz sozinho."
    },
    "owner": { "type": "string", "minLength": 1 }
  }
}
